solved:
    Registration:
        Qr extraction
        Show qr to user
        Session management (get browser session from user)
        Autoregister (get browser data and phone number)

To do:
    Registration:
        Compress files
        Pass to blob (bit64?)
        Encrypt blob
        Upload blob (INSERT)