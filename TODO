solved:
    Registration:
        Qr extraction
        Show qr to user
        Session management (get browser session from user)
        Autoregister (get browser data and phone number)
        Compress files

To do:
    Registration:
        Upload blob (INSERT)

To do (later):
    Put constants
        Handler_Registration_Upload.go ()
    Minimize the folder size (userdata saved in database - I think the key is in the cookies)
    Use filestreamline in Mysql (figure out how it works in the production system)
    Create circular queue of actions system
    Disable headless option

To run:
    go build
    ./WhatsApp_project