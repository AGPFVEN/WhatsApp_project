solved:
    Qr extraction
    Show qr to user

To do:
    Instructions management:
        Design (probably a db aproach)
        Implementation
    Session management (future):
        End chromedp and 

Try:
    autoregister
        at closing chromedp driver, if it is closed gracefuly I can retirve the context to future drivers
        What I need is to make a good LogInHandler function that starts and ends the browser in the same function